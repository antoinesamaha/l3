<project name="budgetProperties">
	<property name="budget" value="budget"/>
	<property name="budget.prjDir" value="${srcRoot}/${budget}"/>
	<property name="budget.srcDir" value="${budget.prjDir}/src"/>
	<property name="budget.exeDir" value="${exeRoot}/${appSubDir}/${budget}"/>
	<property name="budget.buildDir" value="${budget.exeDir}/${buildSubDir}"/>
	<property name="budget.classDir" value="${budget.buildDir}/${classSubDir}"/>
	<property name="budget.jarDir" value="${budget.buildDir}/${jarSubDir}"/>
	<property name="budget.jarFile" value="${budget.buildDir}/${jarSubDir}/01b-${budget}.jar"/>
	<property name="budget.ojarName" value="${budget}.jar"/>
	<property name="budget.packDir" value="${budget.exeDir}/${versionSubDir}"/>
</project>