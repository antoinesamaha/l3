<project name="paymentProperties">
	<property name="payment" value="payment"/>
	<property name="payment.prjDir" value="${srcRoot}/${payment}"/>
	<property name="payment.srcDir" value="${payment.prjDir}/src"/>
	<property name="payment.buildDir" value="${exeRoot}/${appSubDir}/${payment}/${buildSubDir}"/>
	<property name="payment.classDir" value="${payment.buildDir}/${classSubDir}"/>
	<property name="payment.jarDir" value="${payment.buildDir}/${jarSubDir}"/>
	<property name="payment.jarFile" value="${payment.buildDir}/${jarSubDir}/01b-${payment}.jar"/>
	<property name="payment.ojarName" value="${payment}.jar"/>
</project>